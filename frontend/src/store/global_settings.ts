import constants from "@/constants";

const global_settings = {
  state: {
    language: constants.language.en_US,
    theme: constants.theme.light,
  },
  mutations: {},
  actions: {},
};

export default global_settings;
